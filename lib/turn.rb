# Player places a piece (X)

class Turn
  attr_reader :player, :computer

  def initialize(player, computer)
    @piece = piece
  end

  def position_available(column) # if this position is a ".", then replace "." with the piece
    if board[column][5] != "."
# try .find to locate first available position
    end
  end

end
